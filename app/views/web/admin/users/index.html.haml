.page-header
  %h1=t '.title'

.row
  .span4
    = link_to t('.new'), new_admin_user_path, class: 'btn btn-primary'
%br

%table.table.table-striped
  %thead
    %tr
      %th= ham(:user, :id)
      %th= ham(:user, :first_name)
      %th= ham(:user, :last_name)
      %th= ham(:user, :role)
      %th
  %tbody
    - @users.each do |user|
      %tr
        %td= user.id
        %td= user.first_name
        %td= user.last_name
        %td= user.role_text
        %td
          .pull-right.btn-group
            = link_to edit_admin_user_path(user), class: 'btn btn-mini' do
              %i.icon-pencil

